"use strict";

Object.defineProperty(exports, "__esModule", {
    value: true
});
var createUniquePairs = exports.createUniquePairs = function createUniquePairs(array) {
    var pairs = [];

    // Create a clone of the array first so we don't modify the original.
    array = array.slice();

    var _loop = function _loop() {
        var a = array.shift();
        array.forEach(function (b) {
            return pairs.push([a, b]);
        });
    };

    while (array.length > 1) {
        _loop();
    }

    return pairs;
};

exports.default = {
    createUniquePairs: createUniquePairs
};